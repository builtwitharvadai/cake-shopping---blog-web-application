# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.local in the project root
# 2. Fill in the actual values for your environment
# 3. Never commit .env.local to version control
# 4. Restart the development server after making changes
#
# SECURITY NOTES:
# - Keep all sensitive values in .env.local (already in .gitignore)
# - Use NEXT_PUBLIC_ prefix only for client-side accessible variables
# - Never expose API keys, secrets, or credentials in NEXT_PUBLIC_ variables
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Determines the runtime environment
# Options: development | production | test
NODE_ENV=development

# -----------------------------------------------------------------------------
# Next.js Configuration
# -----------------------------------------------------------------------------
# Base URL for the application (used for absolute URLs)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API base URL (publicly accessible in browser)
# Development: http://localhost:3000/api
# Production: https://yourdomain.com/api or https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?[options]
# Example: postgresql://postgres:password@localhost:5432/cake_shop?schema=public
# 
# Docker Usage:
# - When running in Docker Compose, use service name as host: db
# - When running locally, use localhost
# - Default Docker Compose values are provided below
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cakeshop

# Docker Compose PostgreSQL Configuration
# These variables are used by the PostgreSQL container in docker-compose.yml
# Change these values to customize your local database credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cakeshop

# Database connection pool settings (optional)
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# -----------------------------------------------------------------------------
# Kubernetes Configuration
# -----------------------------------------------------------------------------
# Kubernetes namespace for deployment
# Development: development
# Staging: staging
# Production: production
KUBE_NAMESPACE=production

# Kubernetes context/cluster name
# This should match your kubectl context for the target cluster
# Example: production-cluster, staging-cluster, minikube
KUBE_CONTEXT=production-cluster

# Ingress hostname for the application
# This is the domain where your application will be accessible
# Development: cake-shop.local or localhost
# Production: cake-shop.example.com or your actual domain
INGRESS_HOST=cake-shop.example.com

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
# NextAuth.js secret (generate with: openssl rand -base64 32)
# NEXTAUTH_SECRET=your-secret-key-here

# NextAuth.js URL (should match NEXT_PUBLIC_APP_URL)
# NEXTAUTH_URL=http://localhost:3000

# JWT signing key (generate with: openssl rand -base64 32)
# JWT_SECRET=your-jwt-secret-here

# Session configuration
# SESSION_MAX_AGE=2592000

# -----------------------------------------------------------------------------
# External Services
# -----------------------------------------------------------------------------
# Email service (e.g., SendGrid, AWS SES, Resend)
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_SERVER_HOST=smtp.example.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email-user
# EMAIL_SERVER_PASSWORD=your-email-password

# Payment gateway (e.g., Stripe)
# STRIPE_PUBLIC_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# File storage (e.g., AWS S3, Cloudinary)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# -----------------------------------------------------------------------------
# Analytics & Monitoring
# -----------------------------------------------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
# Enable/disable features during development
# NEXT_PUBLIC_ENABLE_BLOG=true
# NEXT_PUBLIC_ENABLE_CHECKOUT=true
# NEXT_PUBLIC_ENABLE_REVIEWS=true

# -----------------------------------------------------------------------------
# Development Tools
# -----------------------------------------------------------------------------
# Enable debug logging
# DEBUG=false

# Disable telemetry
# NEXT_TELEMETRY_DISABLED=1

# -----------------------------------------------------------------------------
# Rate Limiting & Security
# -----------------------------------------------------------------------------
# API rate limiting
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# CORS allowed origins (comma-separated)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# -----------------------------------------------------------------------------
# Cache Configuration
# -----------------------------------------------------------------------------
# Redis connection (if using Redis for caching)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your-redis-password

# Cache TTL in seconds
# CACHE_TTL=3600