apiVersion: v1
kind: ConfigMap
metadata:
  name: cake-shop-config
  namespace: production
  labels:
    app: cake-shop
    component: config
    managed-by: gitops
  annotations:
    description: "Non-sensitive application configuration for cake shop"
data:
  LOG_LEVEL: "info"
  ENVIRONMENT: "production"
  NODE_ENV: "production"
  NEXT_PUBLIC_API_URL: "https://cake-shop.example.com/api"